{
  "dependencies": {
    "highlight.js": "^11.9.0"
  },
  "name": "assets",
  "version": "1.0.0",
  "main": "tailwind.config.js",
  "scripts": {
    "deploy": "cd .. && mix assets.deploy && rm -f _build/esbuild",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Haitham",
  "license": "ISC",
  "description": "Elixir Gist",
  "devDependencies": {
    "@types/phoenix": "^1.6.4",
    "@types/phoenix_live_view": "^0.18.4"
  }
}
